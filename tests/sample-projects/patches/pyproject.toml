[tool.vendoring]
destination = "vendored/"
requirements = "vendor.txt"
namespace = "patches.vendored"

patches-dir = "patches"

[tool.vendoring.transformations]
drop = [
    "*.dist-info",
    "*.egg-info",
]
